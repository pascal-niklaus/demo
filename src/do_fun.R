######################################################################
###
### nonsensical demo functions

do_fun <- function(n) {
    for (i in seq_len(n)) {
        cat("This is fun!\n")
    }
}

more_fun <- function(n) {
    for (i in seq_len(n)) {
        cat("This is more fun !!!\n")
    }
}
